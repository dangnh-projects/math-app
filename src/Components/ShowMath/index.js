import React from 'react';


const ShowMath = props => {
    return (
        <input 
                type="text" 
                value={props.numbers} 
                onChange={() => {}}
            />
    );
}

export default ShowMath;